{
  "installCommand": "npm install && npm install --prefix envy-skin-clone",
  "buildCommand": "npm run build",
  "outputDirectory": "envy-skin-clone/dist",
  "functions": {
    "api/health.ts": {
      "maxDuration": 10
    },
    "api/pix/charge.ts": {
      "includeFiles": "lib/**",
      "maxDuration": 60
    },
    "api/order/[orderId].ts": {
      "includeFiles": "lib/**",
      "maxDuration": 30
    }
  }
}
